#ifndef DEQUE_H
#define DEQUE_H

using namespace std;

class Deque{

private:
    int len;

public:
    // Constructors
    Deque();
    // Deque(int);
    // Deque(float);
    // Deque(double);
    // Deque(char);

    // Destructor
    ~Deque();
};



#endif